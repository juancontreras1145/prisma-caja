name: release-apk

on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
      - name: Descargar repositorio
        uses: actions/checkout@v4

      - name: Configurar Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Configurar Android SDK
        uses: android-actions/setup-android@v3

      - name: Instalar SDK necesario
        run: sdkmanager 'platforms;android-35' 'build-tools;35.0.0'

      - name: Configurar Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.7'

      - name: Verificar secretos de firma
        env:
          PRISMA_KEYSTORE_BASE64: ${{ secrets.PRISMA_KEYSTORE_BASE64 }}
          PRISMA_KEYSTORE_PASSWORD: ${{ secrets.PRISMA_KEYSTORE_PASSWORD }}
          PRISMA_KEY_ALIAS: ${{ secrets.PRISMA_KEY_ALIAS }}
        run: |
          test -n "$PRISMA_KEYSTORE_BASE64" || (echo "Falta el secret PRISMA_KEYSTORE_BASE64" && exit 1)
          test -n "$PRISMA_KEYSTORE_PASSWORD" || (echo "Falta el secret PRISMA_KEYSTORE_PASSWORD" && exit 1)
          test -n "$PRISMA_KEY_ALIAS" || (echo "Falta el secret PRISMA_KEY_ALIAS" && exit 1)

      - name: Crear y validar archivo de firma
        env:
          PRISMA_KEYSTORE_BASE64: ${{ secrets.PRISMA_KEYSTORE_BASE64 }}
          PRISMA_KEYSTORE_PASSWORD: ${{ secrets.PRISMA_KEYSTORE_PASSWORD }}
          PRISMA_KEY_ALIAS: ${{ secrets.PRISMA_KEY_ALIAS }}
        run: |
          printf '%s' "$PRISMA_KEYSTORE_BASE64" | tr -d '\r\n ' | base64 --decode > app/prisma-release.jks
          test -s app/prisma-release.jks || (echo "No se pudo crear app/prisma-release.jks" && exit 1)
          keytool -list -keystore app/prisma-release.jks -storepass "$PRISMA_KEYSTORE_PASSWORD" -alias "$PRISMA_KEY_ALIAS" >/dev/null

      - name: Compilar APK release sin firmar
        run: gradle :app:assembleRelease --no-daemon

      - name: Firmar APK release
        env:
          PRISMA_KEYSTORE_PASSWORD: ${{ secrets.PRISMA_KEYSTORE_PASSWORD }}
          PRISMA_KEY_ALIAS: ${{ secrets.PRISMA_KEY_ALIAS }}
        run: |
          mkdir -p release
          UNSIGNED_APK="$(find app/build/outputs/apk/release -name '*release*.apk' | head -n 1)"
          test -n "$UNSIGNED_APK" || (echo "No se encontro APK release para firmar" && exit 1)
          ZIPALIGN="$ANDROID_HOME/build-tools/35.0.0/zipalign"
          APKSIGNER="$ANDROID_HOME/build-tools/35.0.0/apksigner"
          "$ZIPALIGN" -p -f 4 "$UNSIGNED_APK" release/PrismaCaja-${GITHUB_REF_NAME}-aligned.apk
          "$APKSIGNER" sign \
            --ks app/prisma-release.jks \
            --ks-pass pass:"$PRISMA_KEYSTORE_PASSWORD" \
            --ks-key-alias "$PRISMA_KEY_ALIAS" \
            --key-pass pass:"$PRISMA_KEYSTORE_PASSWORD" \
            --out release/PrismaCaja-${GITHUB_REF_NAME}.apk \
            release/PrismaCaja-${GITHUB_REF_NAME}-aligned.apk
          "$APKSIGNER" verify --verbose release/PrismaCaja-${GITHUB_REF_NAME}.apk

      - name: Publicar APK en GitHub Releases
        uses: softprops/action-gh-release@v2
        with:
          files: release/PrismaCaja-${{ github.ref_name }}.apk
