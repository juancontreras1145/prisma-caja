# Prisma - Experimento seguro imagen al cliente

Esta versión NO reemplaza el botón estable.

Botones:
- Compartir imagen: método estable.
- Enviar al cliente: método experimental con JID de WhatsApp.

Si el experimental falla, usa Compartir imagen.

Mantiene:
- Rescate tacto estable.
- Editar clientes con Agregar WhatsApp.
- JSON nativo estable.
- Clave Ganancias 1145.

## Termux

```bash
cd ~/prisma-caja
unzip -o ~/storage/downloads/prisma_experimento_seguro_imagen_cliente.zip
git add .
git commit -m "Probar envio experimental imagen cliente seguro"
git push
```
